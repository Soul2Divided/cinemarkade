export interface HeroItem {
    id: number;
    tipo: 'pelicula' | 'combo';
    etiqueta: string;
    titulo: string;
    meta: string;
    sinopsis: string;
    imagenBanner: string;
    textoBoton: string;
}
